---
phase: 03-unified-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - skills/morning/SKILL.md
autonomous: true
requirements:
  - USYNC-01
  - USYNC-03

must_haves:
  truths:
    - "Morning ritual calls /sync as its first operational step"
    - "Morning summary shows sync results inline without duplication"
    - "Morning's inbox processing and daily note generation remain unchanged"
    - "User-facing skills are exactly /sync, /morning, and non-sync skills"
  artifacts:
    - path: "skills/morning/SKILL.md"
      provides: "Simplified morning orchestrator delegating sync to /sync"
      contains: "name: morning"
  key_links:
    - from: "skills/morning/SKILL.md"
      to: "skills/sync/SKILL.md"
      via: "Step 2 delegates to /sync skill"
      pattern: "/sync|skills/sync/SKILL.md"
---

<objective>
Simplify `/morning` to delegate all sync work to `/sync` instead of inlining 5 separate sync steps. Morning becomes a 5-step orchestrator: announce, sync (via /sync), process inbox, generate daily note, summary.

Purpose: Eliminates ~180 lines of duplicated sync logic from morning (per D-02), ensures morning shows /sync report inline (per D-03), and completes the unified sync architecture.
Output: Rewritten `skills/morning/SKILL.md` (~100 lines instead of ~280).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-unified-sync/03-CONTEXT.md
@.planning/phases/03-unified-sync/03-RESEARCH.md
@.planning/phases/03-unified-sync/03-01-SUMMARY.md

@skills/morning/SKILL.md
@skills/sync/SKILL.md

<interfaces>
<!-- /sync skill output contract (from Plan 01) -->

/sync produces this output format:
```
Sync completado:

✓ Calendar   — {message}
✓ Jira       — {message}
✓ Slack      — {message}
✗ Reminders  — {message}
✓ Granola    — {message}
─ Training   — {message}

{N} error(es). Revisa {names}.
```

Morning must capture this output verbatim for its [Sync] summary section.

<!-- Morning steps to KEEP unchanged from current SKILL.md -->

Step 7 (Process inbox) — lines 184-206: Interactive inbox processing. User confirms changes. This is the ONLY interactive step.
Step 8 (Generate daily note) — lines 209-234: Core output. MUST succeed. Reads calendar cache, backlog, jira notes, goals, constraints. Generates/updates daily note.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite morning to delegate sync to /sync</name>
  <files>skills/morning/SKILL.md</files>
  <read_first>
    - skills/morning/SKILL.md (current full file — understand Steps 7-8 that must be preserved exactly)
    - skills/sync/SKILL.md (the new /sync skill created in Plan 01 — understand its output format)
    - .planning/phases/03-unified-sync/03-CONTEXT.md (D-02, D-03 locked decisions)
    - .planning/phases/03-unified-sync/03-RESEARCH.md (Pattern 2: Simplified Morning Orchestrator, Pitfall 2: Morning Summary Duplication)
  </read_first>
  <action>
Rewrite `skills/morning/SKILL.md` with this exact structure. The file should be approximately 100 lines (down from 278).

**Frontmatter — KEEP EXACTLY AS-IS:**
```yaml
---
name: morning
description: Use when starting the day, running the morning routine, or chaining all daily preparation. Use when user says "morning", "buenos dias", "manana", "start day", "empieza el dia", "rutina matutina".
---
```

**Step 0 — Load configuration (KEEP AS-IS from current file):**
```
Read `${CLAUDE_PLUGIN_ROOT}/config.yaml`.
If it doesn't exist, tell the user to copy `config.example.yaml` to `config.yaml` and fill in their values. Stop here.

Set `VAULT` = `{config.vault_path}` for all file operations below.
```

**Overview (REWRITE per D-02):**
```
Morning ritual orchestrator. Three phases: (1) sync all data sources via /sync, (2) process inbox interactively, (3) generate daily note. Each phase is independent — if sync fails, inbox processing and daily note still run. The daily note generation is the core output and MUST succeed.
```

**Process section (REWRITE — 5 steps replacing 9):**

### 1. Announce start
```
Buenos dias! Iniciando rutina matutina...
```

### 2. Sync all data sources (per D-02)
Execute the `/sync` skill (defined in `skills/sync/SKILL.md`). This runs all configured connectors (calendar, jira, slack, reminders, granola, training) sequentially and produces a status report.

Capture the full /sync report output — it will be included in the morning summary under the [Sync] section.

If /sync fails entirely (e.g., config.yaml missing connectors_config path): note "Sync no disponible" and continue to Step 3.

### 3. Process inbox
**COPY EXACTLY from current morning/SKILL.md Step 7 (lines 184-206).** This is the only interactive step. The complete logic is:

Read `VAULT/{config.structure.inbox}`:
- If file doesn't exist or is empty (no items below header): skip, note "Inbox vacio"
- If has items:
  1. Read `VAULT/{config.structure.backlog}` for duplicate detection
  2. Read `VAULT/{config.structure.goals}` for tag suggestions
  3. For each inbox item:
     - Reformulate vague items into concrete, actionable tasks
     - Detect duplicates against existing Backlog
     - Suggest tags from `{config.tags.contexts}`, `{config.tags.projects}`, `{config.tags.priority}`, `{config.tags.actionability}`
     - Add wikilinks for people, projects, tickets
  4. Present classification table to user:
     ```
     ## Inbox -> Backlog

     | # | Item original | Tarea propuesta | Tags | Seccion | Nota |
     |---|--------------|-----------------|------|---------|------|
     | 1 | raw item | Reformulated task | #tags | section | |
     ```
  5. Ask: "Aplico estos cambios? Puedes corregir cualquier linea."
  6. On confirmation: move tasks to appropriate `{config.backlog_sections}` in Backlog, clear from Inbox

**This is the only interactive step** — user confirms inbox processing before changes are applied.

**Result:** `X items procesados` | `vacio` | `skipped` (with reason)

### 4. Generate daily note
**COPY EXACTLY from current morning/SKILL.md Step 8 (lines 209-234).** This is the core output — it MUST succeed. The complete logic is:

1. Read all vault data (calendar cache, backlog, jira notes, goals, constraints)
2. Determine focus of the day using goal alignment:
   - Read `VAULT/{config.structure.goals}` (YAML format)
   - Filter goals with status = "in_progress"
   - Score each: `weight * (1 - current/target) * deadline_urgency`
     - deadline_urgency = 1.0 if > 30 days, 1.5 if 14-30 days, 2.0 if < 14 days
   - Cross with today's calendar and backlog #next tasks
   - Pick highest-scoring goal as focus
3. Build top 3 active goals table (by weight)
4. Git safety — pre-mutation:
   - `git -C VAULT add {daily_note_path}` (if exists)
   - `git -C VAULT commit -m "backup: before today mutation"` (continue if nothing to commit)
5. Generate or update daily note at `VAULT/{config.structure.daily_notes}/{date}.md`:
   - If exists: update only Foco, Objetivos activos, and Agenda sections
   - If new: create full template with all sections
6. Git safety — post-mutation:
   - `git -C VAULT add {daily_note_path}`
   - `git -C VAULT commit -m "feat(today): generate daily note {date}"`
7. Add wikilinks for people, projects, Jira tickets

**Result:** `generada` | `actualizada` + path

### 5. Morning summary (REWRITE per D-03 — two sections, no duplication)
Present results in two clear sections. The [Sync] section is the /sync report verbatim. The [Ritual] section covers inbox and daily note only. This prevents duplication (addresses Pitfall 2 from RESEARCH.md).

```
Rutina matutina completada:

[Sync]
{/sync report output verbatim — the "Sync completado:" block with ✓/✗/─ lines}

[Ritual]
- Inbox: [X items procesados/vacio/skipped]
- Daily note: [generada/actualizada] {config.structure.daily_notes}/{date}.md

Foco del dia: [goal-aligned focus with progress]
Reuniones: X hoy
Foco disponible: ~Xh
[any alerts: goal deadlines < 14 days, dragged tasks, overdue items]
```

**CRITICAL — what NOT to include in [Ritual]:** No per-connector sync lines (Calendar, Jira, Slack, Reminders, Granola). Those are already in the [Sync] section from /sync output. Including them again would be the duplication bug from Pitfall 2.

**Graceful Degradation table (REWRITE — simplified):**

| Phase | On failure | Impact |
|-------|-----------|--------|
| Sync (/sync) | Continue to inbox processing | Daily note lacks fresh sync data but still generates |
| Process inbox | Skip if error or empty | Inbox stays dirty, user can run `/process-inbox` manually |
| Generate daily note | MUST succeed | This is the core output — if it fails, report error clearly |

**Important Rules section (KEEP relevant rules, remove sync-specific ones):**
- Each phase is independent — failure in sync NEVER blocks inbox or daily note
- Process inbox is the only interactive step (user confirms changes)
- Daily note generation MUST succeed — it is the core output of the morning ritual
- Spanish language for all user-facing output
- If vault has no config.yaml, stop at Step 0 as usual
- If ALL syncs fail (via /sync), still generate the daily note — it will degrade gracefully with available data
  </action>
  <verify>
    <automated>grep -q "name: morning" skills/morning/SKILL.md && grep -q "description:" skills/morning/SKILL.md && grep -q "/sync" skills/morning/SKILL.md && grep -q "process.inbox\|Process inbox\|Inbox" skills/morning/SKILL.md && grep -q "daily note\|Daily note\|Generate daily" skills/morning/SKILL.md && ! grep -q "### 2. Sync calendar" skills/morning/SKILL.md && ! grep -q "### 3. Sync Jira" skills/morning/SKILL.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - skills/morning/SKILL.md frontmatter contains `name: morning` and `description:` (still user-invocable)
    - File contains reference to `/sync` or `skills/sync/SKILL.md` in Step 2
    - File does NOT contain "### 2. Sync calendar" (old inline sync step removed)
    - File does NOT contain "### 3. Sync Jira" (old inline sync step removed)
    - File does NOT contain "### 4. Sync Slack" (old inline sync step removed)
    - File does NOT contain "### 5. Sync Reminders" (old inline sync step removed)
    - File does NOT contain "### 6. Sync Granola" (old inline sync step removed)
    - File DOES contain inbox processing logic (the interactive step with "Aplico estos cambios?")
    - File DOES contain daily note generation logic (goal scoring, git safety, template generation)
    - Morning summary has [Sync] and [Ritual] sections (per D-03)
    - Morning summary [Ritual] section does NOT have per-connector lines (Calendar, Jira, etc.)
    - File is approximately 100-130 lines (not 278 as before)
    - File has exactly 5 process steps (announce, sync, inbox, daily note, summary)
  </acceptance_criteria>
  <done>Morning SKILL.md delegates all sync to /sync, preserves inbox processing and daily note generation unchanged, and shows a two-section summary (Sync + Ritual) without duplication.</done>
</task>

</tasks>

<verification>
1. `skills/morning/SKILL.md` references /sync for sync delegation
2. `skills/morning/SKILL.md` has NO inline sync steps (calendar, jira, slack, reminders, granola)
3. `skills/morning/SKILL.md` preserves inbox processing with interactive confirmation
4. `skills/morning/SKILL.md` preserves daily note generation with goal scoring
5. Morning summary has [Sync] and [Ritual] sections without duplication
6. `wc -l skills/morning/SKILL.md` shows approximately 100-130 lines (down from 278)
</verification>

<success_criteria>
- `/morning` invokes `/sync` as its first operational step (per D-02)
- Sync report appears inline in morning summary under [Sync] section (per D-03)
- No duplicate sync information in the morning output
- Inbox processing and daily note generation work exactly as before
- Morning is approximately 100 lines instead of 278
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-sync/03-02-SUMMARY.md`
</output>
