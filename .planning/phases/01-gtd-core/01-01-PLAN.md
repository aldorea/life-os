---
phase: 01-gtd-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.example.yaml
  - .planning/phases/01-gtd-core/vault-schema.md
autonomous: true
requirements: [VAULT-01, VAULT-02, VAULT-03]

must_haves:
  truths:
    - "Target vault folder structure is defined with numbered PARA-ish convention"
    - "Frontmatter schemas are documented for every note type (daily, weekly, person, meeting, knowledge)"
    - "Tagging system includes GTD contexts (#home, #office, #calls, #computer) and actionability tags (#next, #esperando, #someday)"
    - "config.example.yaml reflects all new paths, tags, and settings"
  artifacts:
    - path: "config.example.yaml"
      provides: "Updated configuration with new vault structure paths, GTD context tags, project status tags, stalled_days setting"
      contains: "stalled_days"
    - path: ".planning/phases/01-gtd-core/vault-schema.md"
      provides: "Canonical reference for vault structure, frontmatter schemas, and tagging conventions"
      contains: "## Frontmatter Schemas"
  key_links:
    - from: "config.example.yaml"
      to: "vault-schema.md"
      via: "config structure paths match schema folder definitions"
      pattern: "structure:"
---

<objective>
Define the complete vault schema: target folder structure, frontmatter schemas per note type, tagging conventions, and updated config.example.yaml. This is the foundational reference that all subsequent plans depend on.

Purpose: Every skill created or upgraded in Phase 1 needs to know the target paths, frontmatter shapes, and tag vocabulary. Defining this first prevents drift and gives downstream plans a single source of truth.
Output: Updated config.example.yaml + vault-schema.md reference document
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gtd-core/01-CONTEXT.md
@.planning/phases/01-gtd-core/01-RESEARCH.md
@config.example.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config.example.yaml with new vault structure, tags, and settings</name>
  <files>config.example.yaml</files>
  <read_first>
    - config.example.yaml (current state -- must preserve existing keys while adding new ones)
    - .planning/phases/01-gtd-core/01-CONTEXT.md (locked decisions D-01, D-02, D-16)
    - .planning/phases/01-gtd-core/01-RESEARCH.md (target vault structure, tagging convention, config changes needed)
  </read_first>
  <action>
Update config.example.yaml with these specific changes (per D-01, D-02, D-16):

**structure section -- update paths to new numbered PARA convention:**
```yaml
structure:
  daily_notes: "03 Daily"
  weekly_notes: "04 Weekly"
  backlog: "01 Backlog.md"
  inbox: "00 Inbox.md"                          # CHANGED from "02 Inbox.md"
  calendar_cache: ".cache/calendar-cache.md"     # CHANGED from "08 Resources/calendar-cache.md"
  goals: "config/goals.yaml"
  constraints: "config/constraints.yaml"
  jira_notes: "06 Jira"
  people: "05 People"                            # CHANGED from "09 People"
  projects: "02 Projects"                        # CHANGED from "05 Projects"
  training_log: "09 Health/training-log.md"      # CHANGED from "10 Health/training-log.md"
  menus: "09 Health/menus"                       # CHANGED from "10 Health/menus"
  shopping_lists: "11 Nutrition/shopping-lists"   # KEEP (unchanged, but renumber not needed per research)
  meetings: "07 Meetings"                        # CHANGED from "08 Resources/meetings"
  knowledge: "08 Knowledge"                      # CHANGED from "08 Resources/knowledge"
  content_ideas: "08 Knowledge/content-ideas.md" # CHANGED
  content_drafts: "10 Content"                   # CHANGED from "08 Resources/content-drafts"
  granola_processed: ".cache/granola-processed.md" # CHANGED
  claude_memory: ".cache/claude-memory"          # CHANGED from "08 Resources/claude-memory"
  voice: "config/voice.md"
  connectors_config: "config/connectors.yaml"
  archive: "11 Archive"                          # NEW
  cache: ".cache"                                # NEW
```

**tags section -- replace contexts with GTD contexts, add actionability #someday and status tags:**
```yaml
tags:
  contexts: ["#home", "#office", "#calls", "#computer"]
  projects: ["#miportal", "#sherpa", "#previene", "#orbitant", "#lifeos", "#salud"]
  priority: ["#urgente", "#importante"]
  actionability: ["#next", "#esperando", "#someday"]
  status: ["#active", "#stalled", "#completed", "#archived"]
```

**Add new projects section:**
```yaml
projects:
  stalled_days: 14
```

Keep all other sections (daily, weekly, calendar, communication, training, shopping, backlog_sections, contacts) unchanged.

Add "Algún día" filter to backlog_sections if not present (it already exists with empty filter -- keep as is).
  </action>
  <verify>
    <automated>grep -c "stalled_days" config.example.yaml && grep -c "#home" config.example.yaml && grep -c "00 Inbox.md" config.example.yaml && grep -c "11 Archive" config.example.yaml && grep -c ".cache/calendar-cache.md" config.example.yaml</automated>
  </verify>
  <acceptance_criteria>
    - config.example.yaml contains `inbox: "00 Inbox.md"`
    - config.example.yaml contains `people: "05 People"`
    - config.example.yaml contains `projects: "02 Projects"`
    - config.example.yaml contains `meetings: "07 Meetings"`
    - config.example.yaml contains `knowledge: "08 Knowledge"`
    - config.example.yaml contains `archive: "11 Archive"`
    - config.example.yaml contains `cache: ".cache"`
    - config.example.yaml contains `calendar_cache: ".cache/calendar-cache.md"`
    - config.example.yaml contains `granola_processed: ".cache/granola-processed.md"`
    - config.example.yaml contains `contexts: ["#home", "#office", "#calls", "#computer"]`
    - config.example.yaml contains `actionability: ["#next", "#esperando", "#someday"]`
    - config.example.yaml contains `status: ["#active", "#stalled", "#completed", "#archived"]`
    - config.example.yaml contains `stalled_days: 14`
    - All existing sections (daily, weekly, calendar, communication, training, shopping, backlog_sections, contacts) still present
  </acceptance_criteria>
  <done>config.example.yaml has all new paths, GTD context tags, status tags, stalled_days, and archive/cache paths while preserving all existing config sections</done>
</task>

<task type="auto">
  <name>Task 2: Create vault-schema.md canonical reference document</name>
  <files>.planning/phases/01-gtd-core/vault-schema.md</files>
  <read_first>
    - config.example.yaml (just updated in Task 1 -- read for path consistency)
    - .planning/phases/01-gtd-core/01-RESEARCH.md (frontmatter schemas section, tagging convention section)
    - .planning/phases/01-gtd-core/01-CONTEXT.md (D-02, D-03, D-09, D-10, D-11, D-16)
  </read_first>
  <action>
Create `.planning/phases/01-gtd-core/vault-schema.md` as the canonical reference for all downstream plans. This file documents:

**## Target Folder Structure**
The complete tree from RESEARCH.md "Target Vault Structure" section:
```
vault/
  00 Inbox.md
  01 Backlog.md
  02 Projects/
  03 Daily/
  04 Weekly/
  05 People/
  06 Jira/
  07 Meetings/
  08 Knowledge/
  09 Health/
  10 Content/
  11 Archive/
  config/
    goals.yaml
    constraints.yaml
    connectors.yaml
    voice.md
  .cache/
    calendar-cache.md
    granola-processed.md
    claude-memory/
```
Include rationale: lower numbers = higher frequency access.

**## Frontmatter Schemas**
Document strict schemas per note type (per D-03). Copy exact schemas from RESEARCH.md:
- Daily Note: tags*, date*, week*, day*
- Weekly Note: tags*, week*, date_start*, date_end*
- Person: tags*, name*, company?, role?, last_interaction*, created*
- Meeting: tags*, date*, attendees*, source*, project?
- Knowledge Note: tags*, topic*, last_updated*, entries*, maturity*
- Goal (in goals.yaml): id*, name*, dimension*, horizon*, metric*, target*, current*, unit?, weight*, deadline*, status*, history[]

Mark required fields with `*`, optional with `?`.

**## Tagging Convention**
Document all tag categories from updated config.example.yaml:
- GTD Contexts: #home, #office, #calls, #computer (per D-16)
- Projects: #miportal, #sherpa, #previene, #orbitant, #lifeos, #salud
- Priority: #urgente, #importante
- Actionability: #next, #esperando, #someday
- Status: #active, #stalled, #completed, #archived
- Note types: daily, weekly, person, meeting, knowledge

**## Migration Map**
A table showing old path -> new path for each folder/file, so the migration skill can use it directly:
| Old Path | New Path | Action |
|----------|----------|--------|
| 02 Inbox.md | 00 Inbox.md | Rename |
| 05 Projects | 02 Projects | Rename |
| 09 People | 05 People | Rename |
| 08 Resources/meetings | 07 Meetings | Move |
| 08 Resources/knowledge | 08 Knowledge | Move |
| 08 Resources/content-drafts | 10 Content | Move |
| 08 Resources/calendar-cache.md | .cache/calendar-cache.md | Move |
| 08 Resources/granola-processed.md | .cache/granola-processed.md | Move |
| 08 Resources/claude-memory | .cache/claude-memory | Move |
| 10 Health | 09 Health | Rename |
| (new) | 11 Archive | Create |
| (new) | .cache | Create |

Note: 01 Backlog.md, 03 Daily, 04 Weekly, 06 Jira, config/ stay unchanged (numbers happen to match).
  </action>
  <verify>
    <automated>grep -c "## Target Folder Structure" .planning/phases/01-gtd-core/vault-schema.md && grep -c "## Frontmatter Schemas" .planning/phases/01-gtd-core/vault-schema.md && grep -c "## Tagging Convention" .planning/phases/01-gtd-core/vault-schema.md && grep -c "## Migration Map" .planning/phases/01-gtd-core/vault-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - vault-schema.md contains "## Target Folder Structure" with the complete folder tree
    - vault-schema.md contains "## Frontmatter Schemas" with schemas for daily, weekly, person, meeting, knowledge, goal
    - vault-schema.md contains "## Tagging Convention" with all 5 tag categories
    - vault-schema.md contains "## Migration Map" with old->new path table
    - vault-schema.md contains "#home" and "#office" and "#calls" and "#computer" in tagging section
    - vault-schema.md contains "goals.yaml" schema with fields: id, name, dimension, horizon, metric, target, current, weight, deadline, status, history
    - All frontmatter fields marked as required (*) or optional (?)
  </acceptance_criteria>
  <done>vault-schema.md exists as canonical reference with folder structure, all frontmatter schemas, tagging convention, and migration map</done>
</task>

</tasks>

<verification>
- config.example.yaml parseable as valid YAML
- vault-schema.md contains all 4 major sections
- Paths in config.example.yaml match folder names in vault-schema.md
- All GTD context tags present in both files
</verification>

<success_criteria>
- config.example.yaml updated with all new paths, tags, and settings
- vault-schema.md created as downstream reference
- Both files consistent with each other
</success_criteria>

<output>
After completion, create `.planning/phases/01-gtd-core/01-01-SUMMARY.md`
</output>
