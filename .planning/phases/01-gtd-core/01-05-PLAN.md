---
phase: 01-gtd-core
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - skills/next-actions/SKILL.md
  - skills/projects/SKILL.md
  - skills/someday/SKILL.md
autonomous: true
requirements: [GTD-03, GTD-04, GTD-05, GTD-06]

must_haves:
  truths:
    - "User can view next actions filtered by GTD context (#home, #office, #calls, #computer) via /next-actions"
    - "User can view projects list with next action status via /projects"
    - "User can detect stalled projects (no next action or no progress in X days) via /projects"
    - "User can view and review someday/maybe items via /someday"
  artifacts:
    - path: "skills/next-actions/SKILL.md"
      provides: "GTD next actions view with context filtering"
      contains: "## Step 0"
    - path: "skills/projects/SKILL.md"
      provides: "Projects list with stalled detection"
      contains: "stalled"
    - path: "skills/someday/SKILL.md"
      provides: "Someday/maybe list view"
      contains: "## Step 0"
  key_links:
    - from: "skills/next-actions/SKILL.md"
      to: "01 Backlog.md"
      via: "reads backlog tasks and filters by context tag"
      pattern: "config.structure.backlog"
    - from: "skills/projects/SKILL.md"
      to: "02 Projects/"
      via: "reads project files and checks for next actions in backlog"
      pattern: "config.structure.projects"
    - from: "skills/someday/SKILL.md"
      to: "01 Backlog.md"
      via: "reads Algun dia section from backlog"
      pattern: "config.structure.backlog"
---

<objective>
Create three new GTD view skills: /next-actions (context-filtered task view), /projects (project list with stalled detection), and /someday (someday/maybe list review).

Purpose: Per D-07, these are separate dedicated skills for missing GTD views. They complete the GTD workflow alongside capture and processing.
Output: skills/next-actions/SKILL.md, skills/projects/SKILL.md, skills/someday/SKILL.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-gtd-core/01-CONTEXT.md
@.planning/phases/01-gtd-core/01-RESEARCH.md
@config.example.yaml
@skills/status/SKILL.md

<interfaces>
From config.example.yaml (updated by Plan 01):
- structure.backlog: "01 Backlog.md"
- structure.projects: "02 Projects"
- tags.contexts: ["#home", "#office", "#calls", "#computer"]
- tags.actionability: ["#next", "#esperando", "#someday"]
- tags.status: ["#active", "#stalled", "#completed", "#archived"]
- projects.stalled_days: 14
- backlog_sections: last section is "Algun dia" with empty filter

From 01-RESEARCH.md:
- Next actions filter pattern: read Backlog, parse `- [ ]` lines, filter by context tag
- Stalled project detection: no #next task OR last_updated > stalled_days
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /next-actions skill for context-filtered task viewing</name>
  <files>skills/next-actions/SKILL.md</files>
  <read_first>
    - .planning/phases/01-gtd-core/01-RESEARCH.md (next actions filter pattern code example)
    - .planning/phases/01-gtd-core/01-CONTEXT.md (D-07, D-16)
    - skills/status/SKILL.md (reference for read-only skill pattern)
    - config.example.yaml (tags.contexts, structure.backlog)
  </read_first>
  <action>
Create `skills/next-actions/SKILL.md` following the skill boilerplate.

**Frontmatter:**
```yaml
- - -
name: next-actions
description: Use when viewing next actions, filtering tasks by GTD context, or deciding what to do next. Use when user says "next actions", "proximas acciones", "que puedo hacer", "next", "actions", "tasks by context", "/next-actions #home".
- - -
```

**Step 0 -- Load configuration:** Standard pattern. Set VAULT.

**Overview:** Read-only GTD view that shows next actions from the Backlog, optionally filtered by context tag. Does not modify any files.

**Process:**

### 1. Parse filter
Check if user provided a context filter:
- `/next-actions` -- no filter, show all grouped by context
- `/next-actions #home` -- filter by #home context
- `/next-actions #office` -- filter by #office
- Valid contexts: values from `{config.tags.contexts}` (i.e., #home, #office, #calls, #computer)
- If invalid context provided, show available contexts and ask user to choose

### 2. Read Backlog
Read `VAULT/{config.structure.backlog}`. Parse all lines matching `- [ ] ` (unchecked tasks only). Skip checked tasks `- [x]`.

### 3. Filter and group

**If context filter provided:**
Show only tasks containing that context tag. Also show tasks with #next tag that match the context. Sort by priority tags (#urgente first, then #importante, then others).

**If no filter:**
Group tasks by context tag:
```
## Next Actions

### #home (3)
- [ ] Tarea uno #personal #home #next
- [ ] Tarea dos #home
- [ ] Tarea tres #home #esperando

### #office (5)
- [ ] ...

### #calls (1)
- [ ] Llamar a [[Pedro]] #calls #next

### #computer (8)
- [ ] ...

### Sin contexto (X)
- [ ] Tasks without any context tag

- - -
Total: X next actions
#next: Y tareas marcadas como siguiente
#esperando: Z tareas en espera
```

Tasks tagged #next are shown first within each context group and highlighted.
Tasks tagged #esperando are shown last within each group and dimmed with "(esperando)" label.
Tasks tagged #someday are excluded from this view (they belong in /someday).

### 4. Show summary
```
Next Actions — [#context or "Todos"]
Total: X tareas
Por contexto: #home (X), #office (X), #calls (X), #computer (X), sin contexto (X)
```

**Graceful Degradation:**
| Missing | Behavior |
|---------|----------|
| Backlog doesn't exist | "Backlog no encontrado en {path}. Crea tareas primero." |
| No tasks match filter | "No hay next actions para [context]. Prueba otro contexto." |
| No unchecked tasks | "Todas las tareas completadas. Buen trabajo!" |

**Important Rules:**
- Read-only -- NEVER modify any file
- Exclude #someday tasks (those belong in /someday view)
- Exclude checked tasks `- [x]`
- Preserve exact task text (don't reformulate)
- Spanish language for headers and messages
  </action>
  <verify>
    <automated>grep -c "## Step 0" skills/next-actions/SKILL.md && grep -c "#home" skills/next-actions/SKILL.md && grep -c "#someday" skills/next-actions/SKILL.md && grep -c "Read-only" skills/next-actions/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/next-actions/SKILL.md exists with frontmatter `name: next-actions`
    - SKILL.md contains Step 0 config loading pattern
    - SKILL.md supports context filter argument (#home, #office, #calls, #computer)
    - SKILL.md groups tasks by context when no filter provided
    - SKILL.md shows #next tasks first within each group
    - SKILL.md excludes #someday tasks
    - SKILL.md excludes checked tasks `- [x]`
    - SKILL.md references `config.structure.backlog` (not hardcoded)
    - SKILL.md references `config.tags.contexts` for valid context values
    - SKILL.md contains Graceful Degradation table
    - SKILL.md is read-only (no file modifications)
  </acceptance_criteria>
  <done>/next-actions skill shows GTD context-filtered task view from Backlog</done>
</task>

<task type="auto">
  <name>Task 2: Create /projects and /someday skills</name>
  <files>skills/projects/SKILL.md, skills/someday/SKILL.md</files>
  <read_first>
    - .planning/phases/01-gtd-core/01-RESEARCH.md (stalled project detection pattern, someday recommendation)
    - .planning/phases/01-gtd-core/01-CONTEXT.md (D-07 separate dedicated skills)
    - config.example.yaml (structure.projects, projects.stalled_days, backlog_sections)
    - skills/status/SKILL.md (read-only skill pattern reference)
  </read_first>
  <action>
**Create skills/projects/SKILL.md:**

**Frontmatter:**
```yaml
- - -
name: projects
description: Use when viewing projects, checking project health, or detecting stalled projects. Use when user says "projects", "proyectos", "project list", "stalled projects", "proyectos parados".
- - -
```

**Step 0 -- Load configuration:** Standard pattern. Set VAULT.

**Overview:** Read-only view of all projects with health status and stalled detection. Reads project notes from Projects folder and cross-references with Backlog for next actions.

**Process:**

### 1. Read project files
Read all .md files in `VAULT/{config.structure.projects}/`. For each file:
- Read frontmatter (expect: tags, status, last_updated fields)
- Extract project name from filename
- Note the status field value

### 2. Cross-reference with Backlog
Read `VAULT/{config.structure.backlog}`. For each project:
- Search for tasks containing the project's tag (e.g., #lifeos, #sherpa)
- Check if ANY unchecked task has #next tag -- this is the "next action"
- Count total pending tasks for this project

### 3. Detect stalled projects
For each project with status = "active" (or #active tag):
a. **No next action:** If no task has #next tag for this project -> STALLED (reason: "sin next action")
b. **No progress:** If frontmatter `last_updated` is older than `{config.projects.stalled_days}` days -> STALLED (reason: "sin progreso en X dias")
c. Both conditions can be true simultaneously

### 4. Present
```
## Proyectos

| Proyecto | Estado | Next Action | Tareas pendientes | Ultima actualizacion | Alerta |
|----------|--------|-------------|-------------------|---------------------|--------|
| [[Life OS]] | active | "Implementar fase 1" | 5 | hace 3 dias | |
| [[Sherpa]] | active | NINGUNA | 2 | hace 18 dias | PARADO -- sin next action, sin progreso |
| [[Previene]] | active | "Revisar diseño" | 3 | hace 5 dias | |

- - -
Activos: X | Parados: Y | Completados: Z
```

If user says "/projects stalled" -- show only stalled projects.

### 5. Suggest actions for stalled projects
For each stalled project:
```
Proyecto [[Sherpa]] esta parado:
- Sin next action: agrega una con /process-inbox o editando el Backlog
- Sin progreso en 18 dias: actualiza el estado del proyecto o archivalo
```

**Graceful Degradation:**
| Missing | Behavior |
|---------|----------|
| Projects folder empty | "No hay proyectos. Crea notas en {path}." |
| No frontmatter in project file | Treat as active, last_updated unknown |
| Backlog doesn't exist | Skip next action check, show "Backlog no encontrado" |
| stalled_days not in config | Default to 14 days |

**Important Rules:**
- Read-only -- NEVER modify any file
- Show ALL projects, not just stalled ones (unless user filters)
- Stalled is a warning, not an error -- user decides what to do
- Spanish language for all output

- - -

**Create skills/someday/SKILL.md:**

**Frontmatter:**
```yaml
- - -
name: someday
description: Use when reviewing someday/maybe items, the "algun dia" list, or parking lot ideas. Use when user says "someday", "algun dia", "maybe", "someday maybe", "parking lot", "ideas aparcadas".
- - -
```

**Step 0 -- Load configuration:** Standard pattern. Set VAULT.

**Overview:** Read-only view of someday/maybe items from the Backlog's "Algun dia" section. Designed for periodic review (weekly/quarterly).

**Process:**

### 1. Read Backlog
Read `VAULT/{config.structure.backlog}`. Find the section matching the last entry in `{config.backlog_sections}` (the "Algun dia" section). Also find any tasks tagged #someday regardless of section.

### 2. Collect someday items
Combine:
- All items in the "Algun dia" section (unchecked)
- All items tagged #someday in any section (unchecked)
- Deduplicate (same item may appear in both)

### 3. Present
```
## Algun dia / Someday

X items en la lista:

- [ ] Aprender a tocar guitarra #personal #someday
- [ ] Escribir un libro sobre productividad #someday
- [ ] Curso de Machine Learning #professional #someday
...

- - -
Acciones:
- Para promover un item a activo: muevelo al Backlog con /process-inbox o edita directamente
- Para eliminar: marca como [x] completado o eliminalo del Backlog
- Proxima revision: en el weekly review (/weekly-review)
```

### 4. Offer review mode
If user says "/someday review":
Present each item one at a time:
```
1/X: "Aprender a tocar guitarra #personal #someday"
- Promover a activo? (s/n/saltar)
- Si si: pedir contexto tag (#home/#office/#calls/#computer) y mover a seccion apropiada del Backlog
- Si no: mantener en someday
- Si saltar: siguiente item
```

This is a mutating flow -- apply git safety (D-14) before changes and confirm before writing.

**Graceful Degradation:**
| Missing | Behavior |
|---------|----------|
| Backlog doesn't exist | "Backlog no encontrado" |
| No "Algun dia" section | "No hay seccion 'Algun dia' en el Backlog" |
| No #someday tasks | "Lista someday vacia. Usa /dump o /process-inbox para agregar items." |

**Important Rules:**
- Default mode is read-only (just viewing)
- Review mode (/someday review) is mutating -- requires confirmation per D-15
- If review mode mutates, apply git safety per D-14
- Spanish language for all output
  </action>
  <verify>
    <automated>grep -c "## Step 0" skills/projects/SKILL.md && grep -c "stalled" skills/projects/SKILL.md && grep -c "## Step 0" skills/someday/SKILL.md && grep -c "#someday" skills/someday/SKILL.md && grep -c "config.structure.projects" skills/projects/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/projects/SKILL.md exists with frontmatter `name: projects`
    - skills/projects/SKILL.md contains stalled detection logic (no next action + no progress)
    - skills/projects/SKILL.md references `config.projects.stalled_days` for threshold
    - skills/projects/SKILL.md cross-references Backlog for project tasks
    - skills/projects/SKILL.md contains Graceful Degradation table
    - skills/someday/SKILL.md exists with frontmatter `name: someday`
    - skills/someday/SKILL.md reads "Algun dia" section from Backlog
    - skills/someday/SKILL.md also collects #someday tagged items
    - skills/someday/SKILL.md has review mode with promote/keep/skip flow
    - skills/someday/SKILL.md contains git safety for review mode mutations
    - Both skills reference config.structure.* paths (not hardcoded)
  </acceptance_criteria>
  <done>/projects skill shows project list with stalled detection, /someday skill shows someday/maybe items with optional review mode</done>
</task>

</tasks>

<verification>
- All three skills follow established boilerplate pattern
- All skills are read-only by default (except /someday review mode)
- /projects cross-references Backlog for next action detection
- /someday collects from both section and #someday tag
- No hardcoded paths in any skill
</verification>

<success_criteria>
- /next-actions filters tasks by GTD context from Backlog
- /projects shows all projects with stalled detection
- /someday shows someday/maybe items with review capability
</success_criteria>

<output>
After completion, create `.planning/phases/01-gtd-core/01-05-SUMMARY.md`
</output>
