---
phase: 02-external-integrations
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - skills/morning/SKILL.md
autonomous: false
requirements: [SYNC-03, SYNC-04, SYNC-05, SYNC-06]
must_haves:
  truths:
    - "Morning orchestrator calls sync-jira and sync-reminders in its sync chain"
    - "Each connector runs independently -- failure in one never blocks the next"
    - "A broken Jira sync never blocks daily note generation"
    - "All inbox-writing connectors (Jira, Slack, Reminders) flow through 00 Inbox.md"
    - "Stale data is labeled with last-sync timestamp in Jira note frontmatter"
    - "Calendar, Slack, and Granola sync steps continue to work as before"
  artifacts:
    - path: "skills/morning/SKILL.md"
      provides: "Updated morning orchestrator with all 5 sync connectors wired"
      min_lines: 150
  key_links:
    - from: "skills/morning/SKILL.md"
      to: "skills/sync-jira/SKILL.md"
      via: "morning step calls sync-jira logic"
      pattern: "Sync Jira"
    - from: "skills/morning/SKILL.md"
      to: "skills/sync-reminders/SKILL.md"
      via: "morning step calls sync-reminders logic"
      pattern: "Sync Reminders"
    - from: "skills/morning/SKILL.md"
      to: "00 Inbox.md"
      via: "all inbox-writing connectors converge here"
      pattern: "inbox"
---

<objective>
Update the morning orchestrator to wire in the new sync-jira and sync-reminders connectors, completing the unified inbox flow where all synced items converge.

Purpose: With sync-jira (Plan 01) and sync-reminders (Plan 02) created, the morning ritual needs to call them in sequence alongside existing calendar, slack, and granola syncs. This completes SYNC-06 (unified inbox) and confirms SYNC-03/04/05 (existing connectors remain functional).

Output: Updated `skills/morning/SKILL.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-external-integrations/02-CONTEXT.md
@.planning/phases/02-external-integrations/02-RESEARCH.md
@skills/morning/SKILL.md
@skills/sync-jira/SKILL.md
@skills/sync-reminders/SKILL.md

<interfaces>
<!-- Current morning SKILL.md sync steps (from reading the file): -->
Step 2: Sync calendar (existing, uses icalBuddy, writes to .cache/calendar-cache.md)
Step 3: Sync Jira -- currently says "Phase 2 -- skip gracefully" with placeholder
Step 4: Sync Slack -- currently says "Phase 2 -- skip gracefully" with placeholder
Step 5: Sync Granola (existing, uses Granola MCP)
Step 6: Process inbox (interactive, user confirms)
Step 7: Generate daily note (core output, MUST succeed)
Step 8: Morning summary

<!-- The morning orchestrator INLINES sub-skill logic per Phase 1 decision -->
<!-- It does NOT call other skills -- it contains the full logic for each sync step -->

<!-- Sync-jira writes to: -->
- VAULT/{config.structure.jira_notes}/{ticket.key}.md (ticket notes)
- VAULT/{config.structure.inbox} (inbox items for new assignments)

<!-- Sync-reminders writes to: -->
- VAULT/{config.structure.inbox} (inbox items for each reminder)
- VAULT/{config.structure.cache}/reminders-processed.md (dedup registry)

<!-- Unified inbox flow (D-10, D-11, D-12): -->
- Jira: writes new assignments + status changes to inbox (D-05)
- Slack: writes actions/decisions to inbox (existing)
- Reminders: writes captured items to inbox (new)
- Granola: writes to backlog (NOT inbox) -- stays as-is (D-12)
- Calendar: writes to cache (NOT inbox) -- stays as-is (D-12)
- Dedup: wikilink-based, check [[KEY]] in inbox before appending (D-11)
- Sequential execution means each connector sees what prior connectors wrote
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update morning SKILL.md with Jira and Reminders sync steps</name>
  <files>skills/morning/SKILL.md</files>
  <read_first>
    - skills/morning/SKILL.md (MUST read current full content before modifying)
    - skills/sync-jira/SKILL.md (to understand what logic to inline for Jira sync)
    - skills/sync-reminders/SKILL.md (to understand what logic to inline for Reminders sync)
    - .planning/phases/02-external-integrations/02-CONTEXT.md (D-09, D-10, D-12)
    - config.example.yaml (structure paths)
  </read_first>
  <action>
Update `skills/morning/SKILL.md` to replace the Phase 2 placeholders in Steps 3 and 4 with real sync logic, and add a new Step for Reminders sync. Preserve ALL existing steps (calendar, granola, inbox processing, daily note, summary) exactly as they are per D-09.

**Changes to make:**

**Step 3: Sync Jira** -- Replace the current placeholder with inlined sync-jira logic:

```markdown
### 3. Sync Jira

Check if Jira connector is configured in `VAULT/{config.structure.connectors_config}`:
- If no `jira` section or file doesn't exist: skip, note "Jira sync no configurado"
- If configured:
  1. Read `jira.projects` array from connectors config
  2. For each project:
     - Build MCP tool: `mcp__{project.mcp_server}__jira_search`
     - Build JQL: `project = {project.project_key} AND assignee = '{project.assignee_filter or config.daily.assignee_name}' AND status in ({project.sync_statuses joined})`
     - Call MCP jira_search. If fails: warn, skip this project, continue
     - For each ticket: call `mcp__{project.mcp_server}__jira_get_issue` for full details
     - Write/update note at `VAULT/{config.structure.jira_notes}/{ticket.key}.md` with full frontmatter (key, summary, status, assignee, priority, project, sprint, due_date, created, epic, type, last_sync, tags:jira) and body (Relationships section with wikilinks)
     - Re-sync overwrites entire note (Jira is source of truth)
     - For NEW tickets or action-requiring status changes: check inbox for [[ticket.key]] dedup, append if not present:
       `- Jira [[{key}]] -- Asignado: {summary} ({YYYY-MM-DD})` or
       `- Jira [[{key}]] -- Status changed to "{status}": {summary} ({YYYY-MM-DD})`
  3. Enrich with wikilinks (people, projects)

**Result:** `X tickets sincronizados, Y proyectos` | `skipped — no configurado` | `skipped — MCP no disponible`
```

**Step 4: Sync Slack** -- Replace the Phase 2 placeholder with the actual sync logic (inline from sync-slack pattern):

```markdown
### 4. Sync Slack

Check if Slack connector is configured in `VAULT/{config.structure.connectors_config}`:
- If no `slack` section or file doesn't exist: skip, note "Slack sync no configurado"
- If configured:
  1. Read `slack.channels` array from connectors config
  2. For each channel, use Slack MCP to read last 24h messages
  3. Extract: decisions, action items, questions pending, important announcements
  4. Ignore: casual conversation, resolved threads, bot messages
  5. Enrich with wikilinks (match people in `VAULT/{config.structure.people}/`)
  6. Check inbox for existing wikilinks before appending (D-11 dedup)
  7. Append to `VAULT/{config.structure.inbox}`:
     `- Slack #{channel} — [[Persona]]: description (YYYY-MM-DD)`

**Result:** `X items de Y canales` | `skipped — no configurado` | `skipped — MCP no disponible`
```

**Add new Step 5.5 (renumber as Step 5): Sync Reminders** -- Insert BEFORE the existing Granola sync (since Reminders writes to inbox and Granola writes to backlog):

Wait -- looking at the current order: Calendar(2), Jira(3), Slack(4), Granola(5), Inbox(6), Daily(7), Summary(8). Reminders should go after Slack and before Granola to keep all inbox-writing connectors together. But we need to renumber. New order:

- Step 2: Sync calendar (unchanged)
- Step 3: Sync Jira (updated from placeholder)
- Step 4: Sync Slack (updated from placeholder)
- Step 5: Sync Reminders (NEW)
- Step 6: Sync Granola (unchanged, was Step 5)
- Step 7: Process inbox (unchanged, was Step 6)
- Step 8: Generate daily note (unchanged, was Step 7)
- Step 9: Morning summary (updated to include Reminders result)

**New Step 5: Sync Reminders:**

```markdown
### 5. Sync Reminders

Check if Reminders connector is configured in `VAULT/{config.structure.connectors_config}`:
- If no `reminders` section or file doesn't exist: skip, note "Reminders sync no configurado"
- If configured:
  1. Read `reminders.lists` and `reminders.completed` from connectors config
  2. Load processed registry from `VAULT/{config.structure.cache}/reminders-processed.md` (create if missing)
  3. For each configured list, run osascript to fetch reminders:
     ```
     osascript -e 'tell application "Reminders" ... (AppleScript with explicit ISO date construction) ...'
     ```
  4. Filter out already-processed reminders (match name|creation_date against registry)
  5. For each new reminder, check inbox for existing text (D-11 dedup), then append:
     `- Reminder ({list}) -- {name} ({YYYY-MM-DD})`
     Include `[vence: {due_date}]` if due date exists. Include body text if present.
  6. Update processed registry with newly synced entries
  7. If osascript returns "not authorized": print permission instructions, skip

**Result:** `X recordatorios nuevos` | `skipped — no configurado` | `skipped — permiso denegado` | `skipped — no macOS`
```

**Update Step 9 (Morning summary):** Add Reminders line to the results output:
```
Rutina matutina completada:
- Calendario: [synced/skipped + reason]
- Jira: [X tickets de Y proyectos/skipped + reason]
- Slack: [X items de Y canales/skipped + reason]
- Reminders: [X recordatorios nuevos/skipped + reason]
- Granola: [synced/skipped + reason]
- Inbox: [X items procesados/vacio/skipped]
- Daily note: [generada/actualizada] {path}
```

**Update Graceful Degradation table:** Add rows for Jira and Reminders:

| Step | On failure | Impact |
|------|-----------|--------|
| Sync calendar | Skip, note in summary | Daily note lacks calendar data |
| Sync Jira | Skip project or all, note in summary | No Jira updates in daily note |
| Sync Slack | Skip, note in summary | No Slack updates |
| Sync Reminders | Skip, note in summary | Reminders stay in app, not in inbox |
| Sync Granola | Skip | No meeting note updates |
| Process inbox | Skip if error or empty | Inbox stays dirty |
| Generate daily note | MUST succeed | Core output |

**Preserve ALL existing logic** for Steps 2 (calendar), 6 (Granola -- renumbered), 7 (inbox processing -- renumbered), 8 (daily note generation -- renumbered). Do NOT modify these sections at all per D-09. Only change their step numbers.

**Key principle:** Morning orchestrator inlines sub-skill logic (per Phase 1 decision). It does NOT call `/sync-jira` or `/sync-reminders`. It contains the full logic for each step so that a single skill execution handles everything.
  </action>
  <verify>
    <automated>grep "Sync Jira" skills/morning/SKILL.md && grep "Sync Reminders" skills/morning/SKILL.md && grep "Sync Slack" skills/morning/SKILL.md && grep "mcp__" skills/morning/SKILL.md && grep "osascript" skills/morning/SKILL.md && grep "reminders-processed" skills/morning/SKILL.md && grep "Recordatorios" skills/morning/SKILL.md && grep -c "###" skills/morning/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - skills/morning/SKILL.md contains 9 numbered steps (2-9 as process steps after Step 0 and Overview)
    - Step 3 (Sync Jira) contains MCP tool pattern `mcp__{project.mcp_server}__jira_search`
    - Step 3 reads jira.projects from connectors config, not hardcoded
    - Step 3 writes ticket notes to `{config.structure.jira_notes}/` and inbox items to `{config.structure.inbox}`
    - Step 4 (Sync Slack) contains Slack MCP channel scanning with inbox append
    - Step 5 (Sync Reminders) contains `osascript` call for Apple Reminders
    - Step 5 uses processed registry at `{config.structure.cache}/reminders-processed.md`
    - Step 5 handles "not authorized" error with System Settings instructions
    - Step 6 (Sync Granola) is UNCHANGED from current Step 5
    - Step 7 (Process inbox) is UNCHANGED from current Step 6
    - Step 8 (Generate daily note) is UNCHANGED from current Step 7
    - Step 9 (Summary) includes lines for Jira, Slack, Reminders, Granola, Calendar
    - Graceful degradation table has rows for all 5 sync steps + inbox + daily note
    - No sync step can block another -- each has independent failure handling
    - File contains "MUST succeed" for daily note generation step
  </acceptance_criteria>
  <done>Morning orchestrator wires all 5 sync connectors (calendar, jira, slack, reminders, granola) with independent graceful degradation, inlined logic for each, and unified inbox flow where jira/slack/reminders converge in 00 Inbox.md</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all sync skills and morning orchestrator</name>
  <what-built>
    Complete sync connector suite: sync-jira SKILL.md, sync-reminders SKILL.md, updated morning SKILL.md with all 5 connectors wired. connectors.example.yaml with all three sections.
  </what-built>
  <how-to-verify>
    1. Review `skills/sync-jira/SKILL.md` -- confirm it follows the sync-slack pattern, references MCP tools correctly, creates ticket notes with proper frontmatter, and handles failures gracefully
    2. Review `skills/sync-reminders/SKILL.md` -- confirm AppleScript approach, processed registry, inbox format, and permission error handling
    3. Review `skills/morning/SKILL.md` -- confirm all 5 sync steps are present, existing calendar/granola/inbox/daily-note logic is preserved unchanged, and the summary includes all connectors
    4. Review `connectors.example.yaml` -- confirm jira, reminders, and slack sections with clear examples
    5. Try running `/morning` to see the orchestrator in action (syncs will skip if connectors.yaml not configured in your vault, which is expected)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Morning SKILL.md has 9 process steps with all 5 sync connectors
- Jira sync step uses dynamic MCP tool names from connectors config
- Reminders sync step uses osascript with ISO date construction
- Existing sync steps (calendar, granola) preserved verbatim (D-09)
- Graceful degradation table covers all sync steps
- Morning summary output lists all connector results
- No sync failure can block daily note generation
</verification>

<success_criteria>
- All 5 sync connectors (calendar, jira, slack, reminders, granola) wired into morning orchestrator
- Each connector runs independently with its own failure handling
- Unified inbox flow: jira, slack, and reminders all write to 00 Inbox.md
- Calendar and Granola continue writing to cache and backlog respectively (D-12)
- User can run `/morning` and see all sync results in the summary
</success_criteria>

<output>
After completion, create `.planning/phases/02-external-integrations/02-03-SUMMARY.md`
</output>
